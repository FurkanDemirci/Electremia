using Electremia.Dal.Repositories;
using Electremia.Model.Models;

namespace Electremia.Logic.Services
{
    public class SchoolServices
    {
        private readonly Repository<School> _repo;

        public SchoolServices()
        {
            
        }

        // GetSchools(id)
        // Add(model)
        // Edit(model)
        // Delete(model)
    }
}